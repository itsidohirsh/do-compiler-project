prog simple:
	int x;
	set x = 3;

	if (x == 5):
		int y;
		set y = x * 3;
	done
	
	else:
		int w;
		int z;
		set w = 5;
		set z = 28;

		if (w == z):
			set w = z - 1;
		done
		else:
			set w = z;
		done

		char ch;
		set ch = '3';
	done

	int do;
	set do = 0;
	int count;
	set count = 0;

	while (do <= 10):
		set count = count + 1;
		set do = do + 1;
	done

done :)
